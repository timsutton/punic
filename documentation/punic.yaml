
# Example punic.yaml - put this file (or use `punic init`) next to your root project Cartfile.

defaults:
  configuration: Debug
  platform: iOS
  xcode: Xcode 7.3.1
  use-ssh: true
repo-overrides:
  ExampleProject: file:///Users/schwa/Projects/ExampleProject
